<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.mpi_sws.sddr_userspace">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.bluetooth_le" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="org.mpi_sws.sddr_userspace.SDDR_Core_Service" />

        <receiver android:name="org.mpi_sws.sddr_userspace.encounterhistory.EncounterEventReceiver">
        </receiver>

        <provider android:name=".encounterhistory.EncounterHistoryContentProvider"
            android:authorities="org.mpi_sws.sddr_userspace.encounterhistory" />

        <provider android:name=".linkability.LinkabilityContentProvider"
            android:authorities="org.mpi_sws.sddr_userspace.linkability" />

        <receiver android:name="org.mpi_sws.sddr_userspace.Sleeper"/>

    </application>

</manifest>